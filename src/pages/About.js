export default function About() {
  return (
    <div>
      <h2 className="fw-bold mb-3">About</h2>
      <p>
        This Student CRM is a basic React project using Firebase Auth + Firestore.
        It showcases routing, protected pages, forms, and simple CRUD.
      </p>
    </div>
  );
}
